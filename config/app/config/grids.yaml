sylius_grid:
    grids:
        cart_links_cart_link:
            driver:
                name: doctrine/orm
                options:
                    class: "%cart_links.model.cart_link.class%"
                    repository:
                        method: createListQueryBuilder
                        arguments: ["%locale%"]
            sorting:
                code: asc
            limits: [10, 25, 50]
            fields:
                id:
                    type: string
                    label: sylius.ui.id
                    sortable: id
                code:
                    type: string
                    label: sylius.ui.code
                    sortable: code
                translation.name:
                    type: string
                    label: sylius.ui.name
                    sortable: translation.name
                translation.slug:
                    type: string
                    label: sylius.ui.slug
                    sortable: translation.slug
            filters:
                search:
                    type: string
                    label: sylius.ui.search
                    options:
                        fields: [code, translation.name, translation.slug]
            actions:
                main:
                    create:
                        type: create
                        label: cart_links.ui.new_cart_link
                item:
                    update:
                        type: update
                    delete:
                        type: delete
                bulk:
                    delete:
                        type: delete
