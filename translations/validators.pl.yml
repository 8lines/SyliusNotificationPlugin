eightlines_sylius_notification_plugin:
    notification:
        code:
            unique: Istnieje już powiadomienie o takim kodzie.
            not_blank: Kod nie może być pusty.
            min_length: Kod musi mieć co najmniej {{ limit }} znaków.
            max_length: Kod nie może być dłuższy niż {{ limit }} znaków.
            regex: Kod powiadomienia może składać się tylko z liter, cyfr, myślników i podkreśleń.
        action:
            channel_code:
                not_blank: Kod kanału nie może być pusty.
            custom:
                email_from:
                    not_blank: Email nie może być pusty.
                    not_valid: Email jest nieprawidłowy.
                    max_length: Email nie może być dłuższy niż {{ limit }} znaków.
                phone_number_from:
                    not_blank: Numer telefonu nie może być pusty.
                    max_length: Numer telefonu nie może być dłuższy niż {{ limit }} znaków.
                template:
                    not_exist: Szablon nie istnieje.
                    max_length: Szablon nie może być dłuższy niż {{ limit }} znaków.
                recipient:
                    max_length: Odbiorca nie może być dłuższy niż {{ limit }} znaków.
            content:
                subject:
                    not_blank: Temat nie może być pusty.
                    max_length: Temat nie może być dłuższy niż {{ limit }} znaków.
                message:
                    not_blank: Wiadomość nie może być pusta.
